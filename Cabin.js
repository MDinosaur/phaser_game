//placeholder for prolouge
export default class Cabin extends Phaser.Scene {
    constructor() {
        super({ key: 'Cabin' });
    }

   preload (){
//this.load.image('CabinBG', 'assets'); this.load.image('Note', 'assets'); this.load.image('UI', 'assets'); this.load.image('Inventory', 'assets'); this.load.image('Journal', 'assets');
     
   }

   create(){
 //const CabinBG = this.add.image(720, 400, 'CabineBG').setScale(0.65); console.log(`TitleBG: x=${TitleBG.x}, y=${TitleBG.y}`); const placeholder = this.add.image(-,0 'placeholder').setScale(0.6);
    }

   update (){
//probably have constant updates to check if the player has been through the prolouge (taking the note offf the wall)
   }
